/**
 * Filter constants for consistent "All" options across the application
 * These represent filter options that mean "no specific filter applied"
 */

/**
 * "All Assignees" filter option
 * Used when no specific assignee filter is applied
 */
export const ALL_ASSIGNEES_FILTER = {
  id: "all",
  name: "All Assignees",
};

/**
 * "All Objectives" filter option
 * Used when no specific objective filter is applied
 */
export const ALL_OBJECTIVES_FILTER = {
  id: "all",
  name: "All Objectives",
};

/**
 * "All Release Stages" filter option
 * Used when no specific release stage filter is applied
 */
export const ALL_STAGES_FILTER = {
  id: "all",
  name: "All Release Stages",
};
