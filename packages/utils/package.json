{
  "name": "@headnorth/utils",
  "version": "1.0.0",
  "description": "Shared utilities for Head North",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "dependencies": {
    "extensionless": "^1.9.9",
    "pino": "^8.16.0",
    "purify-ts": "^2.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.10",
    "typescript": "^5.0.0",
    "vitest": "^3.2.4"
  },
  "scripts": {
    "build": "node esbuild.config.js && tsc --project tsconfig.declaration.json",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "lint": "eslint . --fix",
    "type-check": "tsc --noEmit"
  },
  "author": "Michael Wagner-Kulovits",
  "license": "MIT"
}
