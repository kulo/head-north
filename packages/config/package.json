{
  "name": "@headnorth/config",
  "version": "1.0.0",
  "description": "Shared configuration files for Head North applications",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "private": true,
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "lint": "eslint . --fix",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "tsup": "^8.5.1",
    "typescript": "^5.9.3",
    "vitest": "^4.0.14"
  },
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=10.0.0"
  },
  "keywords": [
    "config",
    "headnorth",
    "shared",
    "typescript"
  ],
  "author": "Michael Wagner-Kulovits",
  "license": "MIT",
  "dependencies": {
    "@headnorth/types": "file:../../packages/types",
    "purify-ts": "^2.1.2",
    "zod": "^4.1.12"
  }
}
